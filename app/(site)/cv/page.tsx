import {cvSections,profile} from '@/content/profile';
import {pageMetadata} from '@/lib/site';
export const metadata=pageMetadata({title:'Academic CV',description:'Academic CV of Nimra Zahid: MA and MPhil in International Relations, research on Afghanistan and regional security, and over ten years of teaching in Pakistan and England.',path:'/cv'});
export default function CV(){return <section className="page-section"><p className="eyebrow">CURRICULUM VITAE</p><h1>Nimra Zahid</h1><p className="lead">International Relations researcher & educator</p><p className="muted">Northampton, UK · Updated <time dateTime={profile.updated}>{new Intl.DateTimeFormat('en-GB',{day:'numeric',month:'long',year:'numeric',timeZone:'UTC'}).format(new Date(profile.updated))}</time></p><div className="actions no-print"><a href="/cv.pdf" className="button" download>Download PDF CV ↗</a></div><div className="cv-layout"><nav aria-label="CV sections" className="cv-nav">{cvSections.map(s=><a key={s.id} href={`#${s.id}`}>{s.title}</a>)}</nav><div>{cvSections.map(s=><section key={s.id} id={s.id} className="cv-section"><h2>{s.title}</h2>{s.entries.map(e=><article className="cv-entry" key={e.title}><p className="mono">{e.dates}</p><h3>{e.title}</h3>{e.subtitle&&<p className="institution">{e.subtitle}</p>}<p>{e.detail}</p></article>)}</section>)}<p className="notice">Degree verification available on request.</p></div></div></section>}
